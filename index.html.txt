<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Home Setup Solutions | Calgary Home Services</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    :root {
      --primary: #ff8c00;
      --primary-light: #ffb84d;
      --primary-dark: #e67e00;
      --dark: #0a0e27;
      --dark-secondary: #151b3d;
      --gray-800: #1a1f3a;
      --gray-700: #252d4a;
      --gray-600: #3a4460;
      --gray-400: #7a8ab5;
      --light: #e8ecf1;
      --white: #ffffff;
      --accent: #00d4ff;
      --success: #00ff88;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html, body {
      font-family: 'Segoe UI', Roboto, 'Helvetica Neue', sans-serif;
      color: var(--light);
      background: var(--dark);
      line-height: 1.6;
      overflow-x: hidden;
    }

    .container {
      max-width: 1400px;
      margin: 0 auto;
      padding: 0 20px;
    }

    /* Header */
    .site-header {
      background: rgba(10, 14, 39, 0.95);
      backdrop-filter: blur(20px);
      border-bottom: 1px solid rgba(255, 140, 0, 0.1);
      padding: 1.2rem 0;
      position: sticky;
      top: 0;
      z-index: 1000;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
    }

    .nav-container {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .logo {
      display: flex;
      align-items: center;
      gap: 12px;
      cursor: pointer;
      transition: transform 0.3s ease;
    }

    .logo:hover {
      transform: scale(1.05);
    }

    .logo-img {
      height: 50px;
      width: auto;
      object-fit: contain;
      filter: drop-shadow(0 0 10px rgba(255, 140, 0, 0.3));
    }

    .main-nav {
      display: flex;
      gap: 0.5rem;
    }

    .tab-button {
      padding: 0.75rem 1.5rem;
      background: transparent;
      border: 2px solid transparent;
      color: var(--gray-400);
      font-size: 0.95rem;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
      position: relative;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .tab-button:hover {
      color: var(--primary);
      border-bottom-color: var(--primary);
    }

    .tab-button.active {
      color: var(--primary);
      border-bottom: 2px solid var(--primary);
      text-shadow: 0 0 10px rgba(255, 140, 0, 0.5);
    }

    /* Hero */
    .hero {
      background: linear-gradient(135deg, var(--dark-secondary) 0%, var(--gray-800) 50%, var(--dark) 100%);
      color: var(--white);
      padding: 5rem 0;
      position: relative;
      overflow: hidden;
    }

    .hero::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: radial-gradient(circle at 20% 50%, rgba(255, 140, 0, 0.1) 0%, transparent 50%),
                  radial-gradient(circle at 80% 80%, rgba(0, 212, 255, 0.05) 0%, transparent 50%);
      pointer-events: none;
    }

    .hero-inner {
      position: relative;
      z-index: 1;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 3rem;
      align-items: center;
    }

    .hero h1 {
      font-size: 3.2rem;
      margin-bottom: 1.5rem;
      line-height: 1.2;
      background: linear-gradient(135deg, var(--white) 0%, var(--primary-light) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      animation: fadeInUp 0.8s ease;
    }

    .hero p {
      font-size: 1.1rem;
      color: var(--gray-400);
      margin-bottom: 2rem;
      animation: fadeInUp 1s ease 0.2s backwards;
    }

    .hero-actions {
      display: flex;
      gap: 1.5rem;
      margin-bottom: 2rem;
      flex-wrap: wrap;
      animation: fadeInUp 1.2s ease 0.4s backwards;
    }

    .hero-highlight {
      background: linear-gradient(135deg, rgba(255, 140, 0, 0.1) 0%, rgba(0, 212, 255, 0.05) 100%);
      backdrop-filter: blur(20px);
      padding: 2.5rem;
      border-radius: 12px;
      border: 1px solid rgba(255, 140, 0, 0.2);
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
      animation: fadeInUp 1.4s ease 0.6s backwards;
    }

    .hero-highlight h2 {
      color: var(--primary);
      margin-bottom: 1.5rem;
      font-size: 1.6rem;
      text-shadow: 0 0 20px rgba(255, 140, 0, 0.3);
    }

    .hero-highlight ul {
      list-style: none;
    }

    .hero-highlight li {
      padding: 0.75rem 0;
      color: var(--gray-400);
      border-bottom: 1px solid rgba(255, 140, 0, 0.1);
      transition: color 0.3s ease;
    }

    .hero-highlight li:hover {
      color: var(--primary);
      padding-left: 10px;
    }

    .hero-highlight li:last-child {
      border-bottom: none;
    }

    /* Sections */
    .section {
      padding: 4rem 0;
      background: linear-gradient(180deg, var(--dark) 0%, var(--gray-800) 50%, var(--dark) 100%);
      margin-bottom: 0;
      position: relative;
    }

    .section::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 1px;
      background: linear-gradient(90deg, transparent, rgba(255, 140, 0, 0.3), transparent);
    }

    /* Typography */
    h1, h2, h3, h4 {
      margin-bottom: 1.5rem;
      color: var(--white);
    }

    h1 { font-size: 2.8rem; }
    h2 { font-size: 2.2rem; }
    h3 { font-size: 1.5rem; }

    p {
      margin-bottom: 1rem;
      color: var(--gray-400);
    }

    .small {
      font-size: 0.85rem;
      color: var(--gray-600);
    }

    /* Buttons */
    .btn {
      display: inline-block;
      padding: 13px 32px;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 700;
      cursor: pointer;
      transition: all 0.3s ease;
      border: none;
      font-size: 0.95rem;
      text-align: center;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      position: relative;
      overflow: hidden;
    }

    .btn::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: rgba(255, 255, 255, 0.1);
      transition: left 0.3s ease;
      z-index: -1;
    }

    .btn:hover::before {
      left: 100%;
    }

    .btn-primary {
      background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
      color: var(--dark);
      box-shadow: 0 0 20px rgba(255, 140, 0, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.2);
    }

    .btn-primary:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 30px rgba(255, 140, 0, 0.6), inset 0 1px 0 rgba(255, 255, 255, 0.3);
    }

    .btn-secondary {
      background: rgba(58, 68, 96, 0.6);
      color: var(--primary);
      border: 2px solid var(--primary);
      box-shadow: inset 0 0 20px rgba(255, 140, 0, 0.1);
    }

    .btn-secondary:hover {
      background: rgba(255, 140, 0, 0.1);
      transform: translateY(-3px);
      box-shadow: 0 8px 25px rgba(255, 140, 0, 0.3), inset 0 0 20px rgba(255, 140, 0, 0.15);
    }

    .btn-outline {
      background: transparent;
      color: var(--primary);
      border: 2px solid var(--primary);
      box-shadow: inset 0 0 15px rgba(255, 140, 0, 0.05);
    }

    .btn-outline:hover {
      background: rgba(255, 140, 0, 0.1);
      box-shadow: 0 0 20px rgba(255, 140, 0, 0.4), inset 0 0 15px rgba(255, 140, 0, 0.1);
    }

    /* Grid */
    .grid {
      display: grid;
      gap: 2rem;
      margin: 2rem 0;
    }

    .grid.two-col {
      grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    }

    /* Cards */
    .card {
      background: linear-gradient(135deg, rgba(37, 45, 74, 0.5) 0%, rgba(21, 27, 61, 0.3) 100%);
      backdrop-filter: blur(10px);
      padding: 2rem;
      border-radius: 12px;
      border: 1px solid rgba(255, 140, 0, 0.15);
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
      transition: all 0.4s cubic-bezier(0.23, 1, 0.320, 1);
      position: relative;
      overflow: hidden;
    }

    .card::before {
      content: '';
      position: absolute;
      top: -50%;
      right: -50%;
      width: 100%;
      height: 100%;
      background: radial-gradient(circle, rgba(255, 140, 0, 0.15) 0%, transparent 70%);
      opacity: 0;
      transition: opacity 0.4s ease;
    }

    .card:hover {
      transform: translateY(-8px);
      border-color: rgba(255, 140, 0, 0.4);
      box-shadow: 0 15px 40px rgba(255, 140, 0, 0.2), inset 0 1px 0 rgba(255, 255, 255, 0.1);
    }

    .card:hover::before {
      opacity: 1;
    }

    .card h3 {
      margin-top: 0;
      color: var(--primary-light);
      position: relative;
      z-index: 1;
    }

    .card p {
      position: relative;
      z-index: 1;
    }

    /* Tabs */
    .tab-content {
      display: none;
      animation: fadeIn 0.6s ease;
    }

    .tab-content.active {
      display: block;
    }

    /* Contact Form */
    .contact-form {
      background: linear-gradient(135deg, rgba(37, 45, 74, 0.4) 0%, rgba(21, 27, 61, 0.2) 100%);
      backdrop-filter: blur(10px);
      padding: 2.5rem;
      border-radius: 12px;
      border: 1px solid rgba(255, 140, 0, 0.15);
    }

    .form-group {
      margin-bottom: 1.5rem;
    }

    .form-label {
      display: block;
      margin-bottom: 0.75rem;
      font-weight: 600;
      color: var(--primary);
      text-transform: uppercase;
      font-size: 0.85rem;
      letter-spacing: 0.5px;
    }

    .form-input,
    .form-textarea {
      width: 100%;
      padding: 12px 16px;
      border: 1px solid rgba(255, 140, 0, 0.2);
      border-radius: 8px;
      background: rgba(10, 14, 39, 0.5);
      color: var(--light);
      font-family: inherit;
      font-size: 1rem;
      backdrop-filter: blur(10px);
      transition: all 0.3s ease;
    }

    .form-input::placeholder,
    .form-textarea::placeholder {
      color: var(--gray-600);
    }

    .form-input:focus,
    .form-textarea:focus {
      outline: none;
      border-color: var(--primary);
      background: rgba(10, 14, 39, 0.7);
      box-shadow: 0 0 20px rgba(255, 140, 0, 0.2), inset 0 0 10px rgba(255, 140, 0, 0.05);
    }

    /* Contact List */
    .contact-list {
      list-style: none;
      margin: 2rem 0;
    }

    .contact-list li {
      padding: 1rem 0;
      border-bottom: 1px solid rgba(255, 140, 0, 0.1);
      color: var(--gray-400);
    }

    .contact-list a {
      color: var(--primary);
      text-decoration: none;
      transition: all 0.3s ease;
    }

    .contact-list a:hover {
      color: var(--primary-light);
      text-shadow: 0 0 10px rgba(255, 140, 0, 0.4);
    }

    /* Footer */
    .site-footer {
      background: rgba(10, 14, 39, 0.95);
      color: var(--gray-400);
      padding: 3rem 0;
      text-align: center;
      border-top: 1px solid rgba(255, 140, 0, 0.1);
    }

    .footer-inner p {
      margin: 0.5rem 0;
    }

    /* Utilities */
    .center {
      text-align: center;
    }

    .mt-sm { margin-top: 0.5rem; }
    .mt-lg { margin-top: 2rem; }
    .mb-lg { margin-bottom: 2rem; }

    /* Animations */
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    /* Responsive */
    @media (max-width: 768px) {
      .hero-inner {
        grid-template-columns: 1fr;
      }

      .hero h1 {
        font-size: 2.2rem;
      }

      .main-nav {
        flex-direction: column;
        gap: 0.5rem;
      }

      .tab-button {
        padding: 0.6rem 1rem;
        font-size: 0.85rem;
      }

      .hero-actions {
        flex-direction: column;
      }

      .btn {
        display: block;
        width: 100%;
      }

      .contact-form {
        padding: 1.5rem;
      }
    }
  </style>
</head>
<body>
  <header class="site-header">
    <div class="container nav-container">
      <div class="logo" onclick="switchTab('home'); window.scrollTo(0,0);">
        <img src="https://agi-prod-file-upload-public-main-use1.s3.amazonaws.com/cdec01c2-f851-4f1a-9822-af3f60541f7d" alt="Home Setup Solutions" class="logo-img" />
      </div>
      <nav class="main-nav">
        <button class="tab-button active" onclick="switchTab('home'); this.classList.add('active'); document.querySelectorAll('.tab-button').forEach(b => {if(b !== this) b.classList.remove('active');})">Home</button>
        <button class="tab-button" onclick="switchTab('services'); this.classList.add('active'); document.querySelectorAll('.tab-button').forEach(b => {if(b !== this) b.classList.remove('active');})">Services</button>
        <button class="tab-button" onclick="switchTab('customer-portal'); this.classList.add('active'); document.querySelectorAll('.tab-button').forEach(b => {if(b !== this) b.classList.remove('active');})">My Bookings</button>
        <button class="tab-button" onclick="switchTab('staff'); this.classList.add('active'); document.querySelectorAll('.tab-button').forEach(b => {if(b !== this) b.classList.remove('active');})">Staff</button>
        <button class="tab-button" onclick="switchTab('contact'); this.classList.add('active'); document.querySelectorAll('.tab-button').forEach(b => {if(b !== this) b.classList.remove('active');})">Contact</button>
      </nav>
    </div>
  </header>

  <!-- HOME TAB -->
  <div id="home" class="tab-content active">
    <section class="hero">
      <div class="container hero-inner">
        <div class="hero-text">
          <h1>Simplify Your Space</h1>
          <p>
            Home Setup Solutions is your premium Calgary provider for smart home automation, 
            furniture assembly, mounting, moving setup, professional cleaning, and handyman services.
          </p>
          <div class="hero-actions">
            <a class="btn btn-primary"
               href="https://book.squareup.com/appointments/ygqnrdv6a907zu/location/LBJ4C01HMM5JH/services"
               target="_blank" rel="noopener">
              📅 Book Now
            </a>
            <a class="btn btn-secondary"
               href="https://book.squareup.com/appointments/ygqnrdv6a907zu/location/LBJ4C01HMM5JH/services"
               target="_blank" rel="noopener">
              📋 Manage Appointment
            </a>
          </div>
        </div>
        <div class="hero-highlight">
          <h2>⚡ What We Do</h2>
          <ul>
            <li>🔧 Furniture assembly & mounting</li>
            <li>🏠 Smart home & Wi-Fi setup</li>
            <li>📦 Packing, unpacking & organization</li>
            <li>✨ Deep cleaning & carpet refresh</li>
          </ul>
          <p class="service-area" style="margin: 1.5rem 0 0 0; border-top: 1px solid rgba(255,140,0,0.2); padding-top: 1.5rem;">
            📍 Serving Calgary, AB & nearby communities
          </p>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="container">
        <h2 class="center" style="margin-bottom: 3rem;">Why Home Setup Solutions?</h2>
        <div class="grid two-col">
          <div class="card">
            <h3>🛠️ All-In-One Service</h3>
            <p>
              Organization, tech setup, cleaning, and handyman work from one trusted provider.
            </p>
          </div>
          <div class="card">
            <h3>⚡ 24/7 Flexibility</h3>
            <p>
              Evening and weekend appointments designed around your busy schedule.
            </p>
          </div>
          <div class="card">
            <h3>💎 Premium Quality</h3>
            <p>
              Transparent pricing with upfront quotes and professional workmanship.
            </p>
          </div>
          <div class="card">
            <h3>🔐 Secure & Simple</h3>
            <p>
              Square-powered booking, confirmations, and SMS/email reminders.
            </p>
          </div>
        </div>
      </div>
    </section>
  </div>

  <!-- SERVICES TAB -->
  <div id="services" class="tab-content">
    <section style="padding: 4rem 0; position: relative; z-index: 1;">
      <div class="container center">
        <h1 style="margin-bottom: 1rem;">Live Services & Booking</h1>
        <p style="font-size: 1.1rem; color: var(--gray-400);">
          All pricing and availability powered directly by Square Appointments
        </p>
      </div>
    </section>

    <section class="section">
      <div class="container">
        <div style="background: linear-gradient(135deg, rgba(255,140,0,0.1) 0%, rgba(0,212,255,0.05) 100%); backdrop-filter: blur(10px); padding: 2rem; border-radius: 12px; border: 1px solid rgba(255,140,0,0.2); margin-bottom: 2rem;">
          <p style="margin: 0; color: var(--gray-400);">
            <strong style="color: var(--primary);">✦ Real-Time Pricing:</strong> All current prices, services, and availability update automatically when you change them in Square.
          </p>
        </div>

        <div style="border: 2px solid rgba(255,140,0,0.3); border-radius: 12px; overflow: hidden; min-height: 900px; box-shadow: 0 15px 40px rgba(255,140,0,0.15);">
          <iframe
            style="width: 100%; min-height: 900px; border: 0;"
            src="https://book.squareup.com/appointments/ygqnrdv6a907zu/location/LBJ4C01HMM5JH/services"
            title="Home Setup Solutions – Square Services & Booking"
            loading="lazy"
          ></iframe>
        </div>
      </div>
    </section>
  </div>

  <!-- CUSTOMER PORTAL TAB -->
  <div id="customer-portal" class="tab-content">
    <section style="padding: 4rem 0; position: relative; z-index: 1;">
      <div class="container center">
        <h1 style="margin-bottom: 1rem;">My Bookings & History</h1>
        <p style="font-size: 1.1rem; color: var(--gray-400);">
          Track your appointments and view past service photos
        </p>
      </div>
    </section>

    <section class="section">
      <div class="container">
        <div style="background: linear-gradient(135deg, rgba(255,140,0,0.1) 0%, rgba(0,212,255,0.05) 100%); backdrop-filter: blur(10px); padding: 2rem; border-radius: 12px; border: 1px solid rgba(255,140,0,0.2); margin-bottom: 2rem;">
          <h3 style="margin-top: 0; color: var(--primary);">📅 Your Appointments</h3>
          <p style="color: var(--gray-400);">
            Sign in to view upcoming appointments, reschedule (up to 24 hours before), and review past service photos.
          </p>
          <a class="btn btn-primary" href="https://book.squareup.com/appointments/ygqnrdv6a907zu/location/LBJ4C01HMM5JH/services" target="_blank" rel="noopener">
            → Open My Appointments
          </a>
        </div>

        <div class="grid two-col">
          <div class="card">
            <h3>📋 Upcoming Bookings</h3>
            <p>See all scheduled appointments with times, services, and technician info.</p>
          </div>
          <div class="card">
            <h3>📸 Service Photos</h3>
            <p>Review before/after photos and notes from completed jobs.</p>
          </div>
          <div class="card">
            <h3>🔄 Reschedule</h3>
            <p>Change appointment times up to 24 hours before your booking.</p>
          </div>
          <div class="card">
            <h3>⏰ Smart Reminders</h3>
            <p>Automatic text and email alerts 1 day and 1 hour before.</p>
          </div>
        </div>

        <div style="background: linear-gradient(135deg, rgba(255,140,0,0.1) 0%, rgba(0,212,255,0.05) 100%); backdrop-filter: blur(10px); padding: 2rem; border-radius: 12px; margin-top: 2rem; border: 1px solid rgba(255,140,0,0.2); text-align: center;">
          <h3 style="margin-top: 0; color: var(--primary);">New to Home Setup Solutions?</h3>
          <p style="color: var(--gray-400);">Create an account when booking your first appointment.</p>
          <a class="btn btn-outline" href="https://book.squareup.com/appointments/ygqnrdv6a907zu/location/LBJ4C01HMM5JH/services" target="_blank" rel="noopener">
            → Book Your First Service
          </a>
        </div>
      </div>
    </section>
  </div>

  <!-- STAFF TAB -->
  <div id="staff" class="tab-content">
    <section style="padding: 4rem 0; position: relative; z-index: 1;">
      <div class="container center">
        <h1 style="margin-bottom: 1rem;">Staff Dashboard</h1>
        <p style="font-size: 1.1rem; color: var(--gray-400);">
          Clock in/out, manage schedule, upload photos, track availability
        </p>
      </div>
    </section>

    <section class="section">
      <div class="container">
        <div class="grid two-col">
          <div class="card">
            <h3>⏰ Clock In / Out</h3>
            <p style="margin-bottom: 1.5rem;">Track work hours for payroll using Square Timecards.</p>
            <a class="btn btn-primary" href="https://squareup.com/login" target="_blank" rel="noopener" style="width: 100%;">
              → Open Square Team
            </a>
            <p class="small" style="margin-top: 1rem;">
              Use Square POS or Square Team mobile app to punch in/out at shift start and end.
            </p>
          </div>

          <div class="card">
            <h3>📅 Today's Schedule</h3>
            <p style="margin-bottom: 1.5rem;">View all jobs with customer details and service info.</p>
            <a class="btn btn-primary" href="https://squareup.com/login" target="_blank" rel="noopener" style="width: 100%;">
              → View Jobs
            </a>
            <p class="small" style="margin-top: 1rem;">
              Open Square Appointments for job times, addresses, and service requirements.
            </p>
          </div>

          <div class="card">
            <h3>📸 Upload Job Photos</h3>
            <p style="margin-bottom: 1.5rem;">Submit before/after photos for every completed job.</p>
            <a class="btn btn-primary" href="https://drive.google.com" target="_blank" rel="noopener" style="width: 100%;">
              → Shared Folder
            </a>
            <p class="small" style="margin-top: 1rem;">
              Format: CustomerName_Room_Date. Upload both before and after photos.
            </p>
          </div>

          <div class="card">
            <h3>📋 Availability & Time Off</h3>
            <p style="margin-bottom: 1.5rem;">Request time off or update your availability.</p>
            <a class="btn btn-primary" href="https://squareup.com/login" target="_blank" rel="noopener" style="width: 100%;">
              → Contact Management
            </a>
            <p class="small" style="margin-top: 1rem;">
              All scheduling changes require management approval.
            </p>
          </div>
        </div>

        <div style="background: linear-gradient(135deg, rgba(255,140,0,0.1) 0%, rgba(0,212,255,0.05) 100%); backdrop-filter: blur(10px); padding: 2rem; border-radius: 12px; margin-top: 2rem; border: 1px solid rgba(255,140,0,0.2);">
          <h3 style="margin-top: 0; color: var(--primary);">✅ End-of-Job Checklist</h3>
          <ul style="list-style: none; padding: 0;">
            <li style="padding: 0.75rem 0; color: var(--gray-400); border-bottom: 1px solid rgba(255,140,0,0.1);">✓ Complete all tasks</li>
            <li style="padding: 0.75rem 0; color: var(--gray-400); border-bottom: 1px solid rgba(255,140,0,0.1);">✓ Take clear before/after photos</li>
            <li style="padding: 0.75rem 0; color: var(--gray-400); border-bottom: 1px solid rgba(255,140,0,0.1);">✓ Get customer signature (via Square)</li>
            <li style="padding: 0.75rem 0; color: var(--gray-400); border-bottom: 1px solid rgba(255,140,0,0.1);">✓ Upload photos with proper naming</li>
            <li style="padding: 0.75rem 0; color: var(--gray-400); border-bottom: 1px solid rgba(255,140,0,0.1);">✓ Mark job complete in Square</li>
            <li style="padding: 0.75rem 0; color: var(--gray-400);">✓ Do not clock out until leaving site</li>
          </ul>
        </div>
      </div>
    </section>
  </div>

  <!-- CONTACT TAB -->
  <div id="contact" class="tab-content">
    <section style="padding: 4rem 0; position: relative; z-index: 1;">
      <div class="container center">
        <h1 style="margin-bottom: 1rem;">Contact Us</h1>
        <p style="font-size: 1.1rem; color: var(--gray-400);">
          Questions? Reach out directly or book your first appointment
        </p>
      </div>
    </section>

    <section class="section">
      <div class="container">
        <div class="grid two-col">
          <div>
            <h3 style="color: var(--primary); margin-bottom: 1.5rem;">Get in Touch</h3>
            <p style="color: var(--gray-400); margin-bottom: 2rem;">
              Ready to simplify your space? Use the info below or open our online booking system.
            </p>
            <ul class="contact-list">
              <li><strong style="color: var(--primary);">📍 Service Area:</strong><br/> Calgary, Alberta & nearby</li>
              <li><strong style="color: var(--primary);">⏰ Hours:</strong><br/> By appointment, including evenings & weekends</li>
              <li><strong style="color: var(--primary);">📞 Phone:</strong><br/> <a href="tel:8332302933">833‑230‑2933</a></li>
              <li><strong style="color: var(--primary);">📧 Email:</strong><br/> <a href="mailto:customerservice@homesetupsolutions.ca">customerservice@homesetupsolutions.ca</a></li>
            </ul>
            <a class="btn btn-primary" href="https://book.squareup.com/appointments/ygqnrdv6a907zu/location/LBJ4C01HMM5JH/services" target="_blank" rel="noopener" style="width: 100%;">
              📅 Book with Square
            </a>
          </div>

          <div>
            <h3 style="color: var(--primary); margin-bottom: 1.5rem;">Send a Message</h3>
            <form class="contact-form" onsubmit="handleContactForm(event)">
              <div class="form-group">
                <label class="form-label">Name</label>
                <input type="text" class="form-input" id="contact-name" required placeholder="Your name" />
              </div>
              <div class="form-group">
                <label class="form-label">Email</label>
                <input type="email" class="form-input" id="contact-email" required placeholder="your@email.com" />
              </div>
              <div class="form-group">
                <label class="form-label">Phone (optional)</label>
                <input type="tel" class="form-input" id="contact-phone" placeholder="Your phone" />
              </div>
              <div class="form-group">
                <label class="form-label">Message</label>
                <textarea class="form-textarea" id="contact-message" rows="5" required placeholder="Tell us about your project..."></textarea>
              </div>
              <button type="submit" class="btn btn-primary" style="width: 100%;">Send Message</button>
              <p class="small" style="margin-top: 1rem; text-align: center;">
                We'll respond within 24 hours. For immediate booking, use Square above.
              </p>
            </form>
          </div>
        </div>
      </div>
    </section>
  </div>

  <!-- Footer -->
  <footer class="site-footer">
    <div class="container footer-inner">
      <p>© <span id="year"></span> Home Setup Solutions · Calgary, Alberta</p>
      <p class="small">Booking & payments powered by Square Appointments</p>
    </div>
  </footer>

  <script>
    function switchTab(tabName) {
      document.querySelectorAll('.tab-content').forEach(tab => {
        tab.classList.remove('active');
      });
      document.getElementById(tabName).classList.add('active');
      window.scrollTo(0, 0);
    }

    function handleContactForm(event) {
      event.preventDefault();
      
      const name = document.getElementById('contact-name').value;
      const email = document.getElementById('contact-email').value;
      const phone = document.getElementById('contact-phone').value;
      const message = document.getElementById('contact-message').value;
      
      const subject = `New Contact: ${name}`;
      const body = `Name: ${name}%0DEmail: ${email}%0DPhone: ${phone}%0D%0D${message}`;
      
      window.location.href = `mailto:customerservice@homesetupsolutions.ca?subject=${subject}&body=${body}`;
      
      event.target.reset();
    }

    document.getElementById('year').textContent = new Date().getFullYear();
  </script>
</body>
</html>